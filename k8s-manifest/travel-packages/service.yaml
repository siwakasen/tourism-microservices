apiVersion: v1
kind: Service
metadata:
  name: travel-packages-service
  labels:
    app: travel-packages-service
spec:
  type: ClusterIP
  selector:
    app: travel-packages-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    - port: 21001
      targetPort: 21001
      protocol: TCP
      name: grpc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: travel-packages-service
  labels:
    app: travel-packages-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travel-packages-service
  template:
    metadata:
      labels:
        app: travel-packages-service
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - node1
                      - node2
                      - node3
                      - node4
      imagePullSecrets:
        - name: registry-credentials
      containers:
        - name: travel-packages-service
          image: 
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
            - name: grpc
              containerPort: 21001
          envFrom:
            - configMapRef:
                name: travel-packages-config
            - secretRef:
                name: travel-packages-secret
          volumeMounts:
            - name: travel-volume
              mountPath: /app/dist/apps/travel-packages-service/public
              subPath: public
            - name: travel-volume
              mountPath: /app/dist/apps/travel-packages-service/logs
              subPath: logs

      volumes:
        - name: travel-volume
          persistentVolumeClaim:
            claimName: travel-volume
