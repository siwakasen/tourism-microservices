apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  labels:
    app: transaction-service
spec:
  type: ClusterIP
  selector:
    app: transaction-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    - port: 21001
      targetPort: 21001
      protocol: TCP
      name: grpc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction-service
  labels:
    app: transaction-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transaction-service
  template:
    metadata:
      labels:
        app: transaction-service
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - node1
                      - node2
                      - node3
                      - node4
      imagePullSecrets:
        - name: registry-credentials
      containers:
        - name: transaction-service
          image:
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
            - containerPort: 21001
          envFrom:
            - configMapRef:
                name: transaction-config
            - secretRef:
                name: transaction-secret
          volumeMounts:
            - name: travel-volume
              mountPath: /app/dist/apps/transaction-service/logs
              subPath: logs
      volumes:
        - name: travel-volume
          persistentVolumeClaim:
            claimName: travel-volume
