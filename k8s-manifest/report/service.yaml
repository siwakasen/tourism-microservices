apiVersion: v1
kind: Service
metadata:
  name: report-service
  labels:
    app: report-service
spec:
  type: ClusterIP
  selector:
    app: report-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: report-service
  labels:
    app: report-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: report-service
  template:
    metadata:
      labels:
        app: report-service
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - node1
                      - node2
                      - node3
                      - node4
      imagePullSecrets:
        - name: registry-credentials
      containers:
        - name: report-service
          image: 
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: report-config
            - secretRef:
                name: report-secret
          volumeMounts:
            - name: travel-volume
              mountPath: /app/dist/apps/report-service/logs
              subPath: logs
      volumes:
        - name: travel-volume
          persistentVolumeClaim:
            claimName: travel-volume
