

# EXAMPLE WORKFLOW USE CASE:
# Start with core services: docker compose --profile transaction --profile customer up --detach
# Later add more: docker compose --profile employees up --detach
# Scale existing: docker compose --profile transaction up --scale transaction-service=3 --detach
# Add final service: docker compose --profile travel-packages up --detach
# Down scale: docker compose --profile transaction down --scale transaction-service=1 --detach


# TO buil
#docker compose --profile customer --profile employees --profile expenses --profile rent-car --profile travel-packages --profile transaction build --no-cache

# Start all services (if you want them all)
# docker compose --profile customer --profile employees --profile expenses --profile rent-car --profile travel-packages --profile transaction up --detach

services:

  # Customer Service
  customer-service:
    profiles: ["customer"]
    build:
      context: .
      dockerfile: Dockerfile.customer
    environment:
      - NODE_ENV=production
    volumes:
      - travel-volume/private:/app/dist/apps/customer-service/private
    networks:
      - travel-network
    restart: unless-stopped
    deploy:
      replicas: 1

  # Employees Service
  employees-service:
    profiles: ["employees"]
    build:
      context: .
      dockerfile: Dockerfile.employees
    environment:
      - NODE_ENV=production
    networks:
      - travel-network
    restart: unless-stopped
    deploy:
      replicas: 1

  # Expenses Service
  expenses-service:
    profiles: ["expenses"]
    build:
      context: .
      dockerfile: Dockerfile.expenses
    environment:
      - NODE_ENV=production
    networks:
      - travel-network
    restart: unless-stopped
    deploy:
      replicas: 1

  # Rent Car Service
  rent-car-service:
    profiles: ["rent-car"]
    build:
      context: .
      dockerfile: Dockerfile.rent-car
    environment:
      - NODE_ENV=production
    volumes:
      - travel-volume/public:/app/dist/apps/rent-car-service/public
    networks:
      - travel-network
    restart: unless-stopped
    deploy:
      replicas: 1

  # Travel Packages Service
  travel-packages-service:
    profiles: ["travel-packages"]
    build:
      context: .
      dockerfile: Dockerfile.travel-packages
    environment:
      - NODE_ENV=production
    volumes:
      - travel-volume/public:/app/dist/apps/travel-packages-service/public
    networks:
      - travel-network
    restart: unless-stopped
    deploy:
      replicas: 1

  # Transaction Service
  transaction-service:
    profiles: ["transaction"]
    build:
      context: .
      dockerfile: Dockerfile.transaction
    environment:
      - NODE_ENV=production
    networks:
      - travel-network
    restart: unless-stopped
    deploy:
      replicas: 1

networks:
  cloudflare-network:
    external: true
  travel-network:
    external: true

volumes:
  travel-volume:
    external: true    